import styled from '@emotion/styled';

export const MyReviewContainer = styled.section`
  width: 100%;
  overflow-y: scroll;
  &::-webkit-scrollbar {
    display: none;
  }
`;

export const ReviewFeed = styled.section``;
